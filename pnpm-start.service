[Unit]
Description=pnpm start service
After=network.target git-pull.service

[Service]
User=myusername
WorkingDirectory=/home/pi/git/gethomepage
ExecStart=/usr/local/bin/pnpm start
Restart=always

[Install]
WantedBy=default.target
